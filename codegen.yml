overwrite: true
schema: "./services/main/schema.graphql"
documents: null
generates:
  services/main/types/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
